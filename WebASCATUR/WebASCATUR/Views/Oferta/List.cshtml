@model OfertasListViewModel

@{
    //ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutGallery.cshtml";
}

<div class="row marginTop">
    @foreach (var oferta in Model.Ofertas)
    {
        @Html.Partial("OfertasSummary", oferta)
    }
</div>