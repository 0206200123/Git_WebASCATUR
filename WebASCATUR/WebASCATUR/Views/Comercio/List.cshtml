@model ComercioListViewModel

@{
    //ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutGallery.cshtml";
}

<div class="row marginTop">
    @foreach (var comercio in Model.Comercios)
    {
        @Html.Partial("ComercioSummary", comercio)
    }
</div>